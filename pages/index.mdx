---
title: Home
---
import Image from 'next/image'
import Link from 'next/link'
import Head from 'next/head'

<Head>
  <script
    type="application/ld+json"
    dangerouslySetInnerHTML={{
      __html: JSON.stringify({
        "@context": "http://schema.org",
        "@type": "WebSite",
        "name": "Boardflare",
        "url": "https://www.boardflare.com"
      })
    }}
  />
</Head>

<div>
    <div className="text-8xl font-bold text-center tracking-tight text-blue-900 mt-16 mb-12">
        <h1 className="flex flex-col items-center">
            <span>AI as a Function</span>
            {/* <span className="text-gray-500 text-4xl">for Excel and Sheets</span> */}
        </h1>
    </div>
    <div className="flex justify-center space-x-20 mb-12" >
        <Image src="/images/Microsoft_Office_Excel.svg" alt="Excel logo" width={120} height={120} />
        {/* <span className="text-6xl flex items-center justify-center">🤗</span> */}
        {/* <Image src="/images/csv.svg" alt="CSV icon" width={84} height={84} /> */}
        <Image src="/images/Google_Sheets_logo.svg" alt="Google Sheets logo" width={84} height={84} />
    </div>
</div>

Our apps for Excel and Sheets provide custom functions to perform AI tasks such as translation, sentiment analysis, fuzzy matching, etc., e.g. `=BOARDFLARE.TRANSLATE("Hello", "en", "fr")`.  

AI models running in the browser are 100% free to use.  Premium models with enhanced capabilites that run in the cloud are available with a subscription.

<div className="flex justify-center text-lg">
    
</div>

<div className="flex justify-center mt-10">
    <Link href="/apps/excel/translate">
        <button className="bg-green-600 hover:bg-green-800 text-white font-bold py-2 px-4 rounded">
            Get Started
        </button>
    </Link>
</div>