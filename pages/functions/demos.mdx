import GradioApp from '@/components/Gradio.jsx';

# Python Demos

These demos enable you to test the Python functions directly using Gradio running in your browser with Pyodide.  It takes a few seconds to load the Python environment in your browser, but this only happens once.

## Text

### fuzzy

<GradioApp />